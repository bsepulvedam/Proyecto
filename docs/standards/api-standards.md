# Estándares API

Conservar contratos existentes. Nuevos JSON bajo `/api` y versionar cuando haya consumidores estables. Schemas separados de ORM, códigos HTTP precisos, errores con código estable/mensaje seguro/campos y paginación consistente. Autenticación 401, autorización 403, validación 422/400 según contrato, conflicto 409. Documentar permiso, inputs, outputs y errores; probar casos negativos.
