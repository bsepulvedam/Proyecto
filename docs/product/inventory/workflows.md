# Flujos de Inventario

Actual: importar/crear producto → inicializar transición → recibir → ledger → stock/costo. Objetivo: entrada/recepción → lote/costo/vencimiento → disponibilidad por bodega → reserva opcional → salida FEFO/FIFO aprobada → ledger/auditoría → ajuste compensatorio.

No implementar el flujo objetivo hasta decidir bodegas, lotes, método de costo y permisos.
