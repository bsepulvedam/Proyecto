# Reglas de `frontend/`

Este directorio es un prototipo histórico, no la UI servida. No implementar aquí una funcionalidad operativa sin una decisión explícita. La UI activa vive en `app/templates` y `app/static`. Si se conserva el prototipo, evitar que diverja silenciosamente y documentar cualquier uso de referencia.
