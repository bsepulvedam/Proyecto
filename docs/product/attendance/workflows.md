# Flujos de Asistencia

Actual: trabajador consulta calendario → crea justificación → consulta/descarga su archivo. ADMIN administra lugares y asignaciones.

MVP propuesto: resolver trabajador y planificación → solicitar consentimiento/permisos del navegador → capturar evento y precisión → evaluar lugar/radio → persistir captura y resultado → confirmar → revisión de incidencias. Fallos de GPS no deben convertirse automáticamente en ausencia.
